<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

    <context:property-placeholder location="classpath:config.properties" />
    <bean id="ssoAuthServer" class="songm.account.service.SSOAuthServer"
       init-method="init">
       <property name="key" value="${songm.sso.key}"></property>
       <property name="secret" value="${songm.sso.secret}"></property>
       <property name="host" value="${songm.sso.host}"></property>
       <property name="port" value="${songm.sso.port}"></property>
    </bean>
    <context:component-scan base-package="songm.account" />

    <import resource="application-dao.xml" />
    <import resource="application-mvc.xml" />

</beans>
